#!/usr/bin/expect -f
set timeout 20

spawn env HOST=127.0.0.1 PORT=18080 uv run --no-sync server
expect {
    "Uvicorn running" {
        send "\003"
        expect eof
        exit 0
    }
    timeout {
        send "\003"
        exit 1
    }
}
